interface Project {
    name?: string;
    cm_address: string;
    creator?: [any];
    latest_update: Date;
    description?: string;
    nfts_minted?: [any];
    image?: string;
}

export default Project;
